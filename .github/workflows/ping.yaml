# name: Ping Render Apps

# on:
#   schedule:
#     - cron: "*/5 * * * *" # every 5 minutes
#   workflow_dispatch:

# jobs:
#   ping:
#     runs-on: ubuntu-latest

#     env:
#       PING_URLS: |
#         https://creativehunt.onrender.com
#         https://link-for-feedback-backend.onrender.com

#     steps:
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.x"

#       - name: Install dependencies
#         run: pip install requests

#       - name: Create ping script
#         run: |
#           cat <<EOF > ping.py
#           import requests, time, os

#           urls = os.environ["PING_URLS"].split()
#           for url in urls:
#               try:
#                   res = requests.get(url, timeout=10)
#                   print(f"[{time.ctime()}] Pinged {url} - Status: {res.status_code}")
#               except Exception as e:
#                   print(f"[{time.ctime()}] Ping failed for {url}: {e}")
#           EOF

#       - name: Run ping script
#         run: python ping.py
